{
  "name": "mass-athletic",
  "version": "0.1.0",
  "scripts": {
    "ng": "ng",
    "start": "(cd ui && ng serve --proxy-config proxy.json) & (npx nodemon)",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "starting": "nodemon --exec babel-node ../index.js"
  },
  "private": true,
  "dependencies": {
    "esbuild-register": "^3.4.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
