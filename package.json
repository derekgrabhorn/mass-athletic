{
  "name": "mass-athletic",
  "version": "0.1.0",
  "scripts": {
    "ng": "ng",
    "start": "(cd ui && ng serve --proxy-config proxy.json) & (cd server && nodemon ./server.ts)",
    "build": "ng build",
    "test": "ng test",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "starting": "nodemon --exec babel-node ../index.js"
  },
  "private": true
}
