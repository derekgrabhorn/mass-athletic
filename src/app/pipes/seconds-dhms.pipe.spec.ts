import { SecondsDhmsPipe } from './seconds-dhms.pipe';

describe('SecondsDhmsPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsDhmsPipe();
    expect(pipe).toBeTruthy();
  });
});
